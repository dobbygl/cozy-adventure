# Copy to .env and fill in. .env is gitignored.

# OpenAI — used by scripts/gen-images.mjs to generate the landing art.
OPENAI_API_KEY=
# Image model for generation (see scripts/gen-images.mjs).
OPENAI_IMAGE_MODEL=gpt-image-2
